# Legacy GUI Notes

The files in this folder describe the previous browser-based PBE MVP direction.

That direction is deprecated. Current Project Blueprint Engine work targets a Codex Plugin that runs inside Codex and stores artifacts in `.pbe/`.

Do not extend the React/Vite GUI unless the project direction changes again.
