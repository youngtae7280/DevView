# Deprecated GUI Source

The React/Vite GUI implementation in `src/` belongs to the previous Project Blueprint Engine direction.

Current work targets the Codex Plugin structure at the repository root:

```text
.codex-plugin/
skills/
templates/
schemas/
docs/
```

Do not extend the GUI code path unless the project direction changes again.
