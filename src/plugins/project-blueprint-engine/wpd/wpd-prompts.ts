export const WPD_PROMPT_BOUNDARY = [
  'Convert requirement-tree leaves into executable work design.',
  'Do not ask the user for priority, risk, or work-unit metadata during RPD.',
  'Use confirmed_leaf nodes and extracted facts as planning inputs.',
  'Keep scope and non-scope explicit enough for an autonomous coding pass.',
].join('\n')
